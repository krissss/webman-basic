<?php
// @formatter:off

namespace PHPSTORM_META {

    override(\support\Container::make(0), map([
        '' => '@',
    ]));
    override(\support\Container::get(0), map([
        '' => '@',
    ]));
}
